/* ============== RELATED PAPER (independent card under article) ============== */
.related-paper {
  position: relative;
  padding: 26px 28px 28px;
  z-index: 1;
}

.related-paper::before {
  content: "";
  position: absolute;
  top: 0; left: 0; right: 0; bottom: 0;
  background: var(--paper);
  z-index: -1;
  border-radius: 4px;
  filter: url(#paper-edge) drop-shadow(0 2px 6px rgba(0,0,0,0.06));
}

.related-paper::after {
  content: "";
  position: absolute;
  top: -10px;
  right: 30px;
  z-index: 2;
  width: 70px;
  height: 20px;
  background-color: var(--tape-blue);
  background-image: var(--tape-stripes);
  transform: rotate(6deg);
}

.related-paper__head {
  display: flex;
  align-items: baseline;
  justify-content: space-between;
  gap: 12px;
  margin-bottom: 18px;
}

.related-paper__head h2 {
  display: inline-flex;
  align-items: center;
  gap: 10px;
  margin: 0;
  font-size: 17px;
  font-weight: 700;
}

.related-paper__head h2 .lucide {
  width: 18px;
  height: 18px;
  color: var(--color-accent-strong);
}

.dark-mode .related-paper__head h2 .lucide {
  color: var(--color-accent-muted);
}

.related-paper__more {
  display: inline-flex;
  align-items: center;
  gap: 4px;
  color: var(--muted);
  font-size: 13px;
  transition: color 0.15s ease;
}

.related-paper__more .lucide {
  width: 13px;
  height: 13px;
}

.related-paper__more:hover {
  color: var(--color-accent-strong);
}

.related-paper__grid {
  display: grid;
  grid-template-columns: repeat(4, minmax(0, 1fr));
  gap: 14px;
}

/* Column collapse for .related-paper__grid lives in _layout/responsive.styl
   (1200px / 600px), with the rest of the theme breakpoints. */

.related-card {
  position: relative;
  z-index: 1;
  display: flex;
  flex-direction: column;
  color: var(--ink);
  text-decoration: none;
  box-shadow: 0 2px 6px rgba(0,0,0,0.06);
  transition: transform 0.22s ease;
}

.related-card::before {
  content: "";
  position: absolute;
  top: 0; left: 0; right: 0; bottom: 0;
  background: var(--paper);
  z-index: -1;
  border-radius: 4px;
  filter: url(#paper-edge);
}

.related-card::after {
  content: "";
  position: absolute;
  top: 0; left: 0; right: 0; bottom: 0;
  z-index: -2;
  border-radius: 4px;
  box-shadow: var(--small-shadow);
  opacity: 0;
  pointer-events: none;
  transition: opacity 0.22s ease;
}

.related-card:hover {
  transform: translateY(-3px);
}

.related-card:hover::after {
  opacity: 1;
}

.related-card__thumb {
  display: block;
}

.related-card__thumb .paper-thumb {
  height: 110px;
  border-radius: 4px 4px 0 0;
}

.related-card__thumb .paper-thumb:not(.paper-thumb--cover) {
  filter: url(#paper-edge);
}

.related-card__thumb .paper-thumb::after { display: none; }

.related-card__body {
  display: flex;
  flex-direction: column;
  gap: 4px;
  padding: 10px 12px 12px;
}

.related-card__title {
  font-size: 13px;
  font-weight: 600;
  line-height: 1.45;
  display: -webkit-box;
  -webkit-line-clamp: 2;
  -webkit-box-orient: vertical;
  overflow: hidden;
  word-break: break-word;
}

.related-card:hover .related-card__title {
  color: var(--color-accent-strong);
}

.dark-mode .related-card:hover .related-card__title {
  color: var(--color-accent-muted);
}

.related-card__date {
  color: var(--muted);
  font-size: 11px;
  font-style: normal;
  font-variant-numeric: tabular-nums;
}
